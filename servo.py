import serial

#НОМЕР COM ПОРТА
# ser = serial.Serial('COM6', 1200)
# ser.close()




def move_camera_to_target(dx, dy):
    # Determine the direction and amount of movement required
    if dx == 0 and dy == 0:
        print('Nice')
        return bytearray([0xFF, 0x01, 0x00, 0x00, 0x00, 0x00, 0x01])

    if dx < 0: # + право
        return bytearray([0xFF, 0x01, 0x00, 0x02, 0x64, 0x00, 0x67])
    elif dx > 0: # - лево
        return bytearray([0xFF, 0x01, 0x00, 0x04, 0x64, 0x00, 0x69])
    elif dy < 0: # + вниз
        return bytearray([0xFF, 0x01, 0x00, 0x10, 0x64, 0x00, 0x75])
    elif dy > 0: # - вверх
        return bytearray([0xFF, 0x01, 0x00, 0x08, 0x64, 0x00, 0x6D])